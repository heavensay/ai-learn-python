[project]
name = "demo"
version = "0.1.0"
description = "demo"
readme = "README.md"

[tool.uv]
package = false

requires-python = ">=3.14"
dependencies = [
    "matplotlib",
]

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
